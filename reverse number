#include<stdio.h>
int main()
{
int r,q,rev ,no;
rev=0;
printf("enter a number :");
scanf("%d",&no);
while(no>0)
{
r=no%10;
q=no/10;
printf("r=%d,q=%d, rev=%d\n",r,q,rev);
rev=rev*10+r;
no=q;
}
printf("reverse number is %d",rev);
return 0;
}
